# Lesson 1

This lesson shows the smallest useful Doxygen project for embedded C:

- one documented C header
- one C source file
- one intentionally undocumented function
- one struct, one enum, and a few APIs with pointer parameters

After you run Doxygen, compare what appears in the generated HTML with what is
present in `include/math_utils.h`.
